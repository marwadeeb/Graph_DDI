# Raw XML input (too large for git)
*.xml

# Step 1 & 2 outputs — local only, not pushed to GitHub
data/step1_full/
data/step2_dedup/

# RAG index — too large for GitHub (~2.5 GB)
data/rag_index/

# Evaluation results — local only
data/evaluation/*.csv
data/evaluation/rag_eval*
data/evaluation/*.npz
#    (keep JSON results tracked, ignore large CSVs)

# Step 3 approved subset IS tracked (web app needs it)
# data/step3_approved/

# Step 4 graph data — tracked locally, but not needed by the web server.
# Uncomment the line below to exclude from HF Spaces push:
# data/step4_graph/

# Python
__pycache__/
*.py[cod]
*.pyo
.env
venv/
.venv/


# OS
.DS_Store
Thumbs.db

docs/EECE690-RubricReport.pdf